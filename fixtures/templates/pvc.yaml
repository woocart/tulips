apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: test-volume
  labels:
    app: test
spec:
  storageClassName: ssd
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi
